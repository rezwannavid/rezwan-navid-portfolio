# Use a long, random value in production. This signs session-only access cookies.
WORK_ACCESS_SECRET=replace-with-a-long-random-secret

# Passwords remain server-side and are never included in the browser bundle.
WORK_PASSWORD_RIDECENTRIC=replace-with-the-shared-project-password
